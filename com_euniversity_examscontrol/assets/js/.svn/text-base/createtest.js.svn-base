jQuery('#checkAllBtn').live('click', function (e) {
    if (jQuery(this).attr('checked')) {
        jQuery('input[type=checkbox]',jQuery(this).parents('table:first')).attr('checked',true);
    }else{
        jQuery('input[type=checkbox]',jQuery(this).parents('table:first')).attr('checked',false);
    }
});
